module.exports = function (grunt) {
//	grunt.registerTask('default', ['compileAssets', 'linkAssets',  'watch']);
//  grunt.registerTask('default', ['clean:dev','copy:dev']);
  grunt.registerTask('default', ['clean:dev','copy:dev', 'watch']);

};
